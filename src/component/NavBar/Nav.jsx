import React from "react";
import { Box } from "@mui/material";

export const Nav = () => {
  return (
    <Box
      sx={{
        background: "#121231",
        borderRadius: "16px",
        color: "#eee",
        padding: "2rem",
      }}
    >
      
    </Box>
  );
};
